[package]
name = "jmux-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
toml = "0.8"
dirs-next = "2"

[dev-dependencies]
tempfile = "3"
